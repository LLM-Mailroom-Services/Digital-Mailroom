# TASKS.md — the Digital-Mailroom task board

The single source of truth for cross-agent task state in the **Digital-Mailroom
standalone repo** (`LLM-Mailroom-Services/Digital-Mailroom`): what is
**assigned**, **in progress**, **needs attention**, and **done**. Every agent
(and human contributor) reads this board before working and keeps it current
while working. It is a WORKING DOCUMENT, not documentation — modify it as work
happens, never delete history (finished cards move to the Archive at the bottom).

**Lineage (DMR-001, 2026-09-09):** this board was restarted fresh for the
standalone Digital-Mailroom version. The predecessor board — the `HUB-*` cards
of the original `Exios66/mailroom-dev` monorepo — remains canonical for that
repo; HUB-era history and evidence live there and in this repo's git history
(`git log --follow -- governance/TASKS.md`). The `DMR-` namespace starts at
DMR-001 and never reuses HUB numbers.

**Time convention (hub#plan-Phase7):** all dates on this board are **UTC**.
Owner-cell dates and archive-row dates are bare `YYYY-MM-DD` and are
interpreted as UTC (the tooling emits ISO-8601 UTC — `%Y-%m-%dT%H:%M:%SZ`).
Never write local-time timestamps into evidence or Owner cells; when a precise
instant is needed use the ISO-8601 UTC form. `scripts/board_state.py check`
surfaces Owner-cell/archive dates that carry no UTC marker as info findings.

Scope: **this repo's monorepo-level work** (workspace wiring, cross-package
governance, board tooling, docs, releases). Work scoped to a single package is
tracked by that package's own governance — this board holds the hub view and
cross-package cards.

**Machine-readable state (DMR-001):** this board is computationally readable
via `python scripts/board_state.py` — `status` (snapshot, `--json` for
machines), `card DMR-0NN` (one card + its commits), `check` (invariants:
structural contradictions exit 1; hygiene drift is warned), `sync-issues`
(label sync onto synced issues), `pull-issues` (import served-board lane moves
back into this file), `project-init`/`project-sync` (GitHub Projects v2
mirror). The label taxonomy for issues lives in `.github/labels.json`
(`stage/*` = these lanes, `attention/*` = the needs_attention tags, `type/*`,
`priority/*`, `domain/*`, `kanban` marker); the CI gate
`.github/workflows/board-governance.yml` runs `check` + the label audit on
every change to `governance/`, `scripts/`, or `.github/`.

**Served board:** the live, issue-backed dispatch board for this repo is
**https://digital-mailroom-theta.vercel.app** (Vercel project
`digital-mailroom`, deploy root `board-site/`, `GITHUB_TOKEN` production
secret). It reads `kanban`-labeled issues in
`LLM-Mailroom-Services/Digital-Mailroom`; writes (move/edit/archive) PATCH
back to those issues. The canonical board stays this file — run
`board_state.py pull-issues` to import site-side lane moves, `sync-issues` to
push this file's truth into the issues.

## How to use — four steps

1. **Read first.** Read this board (and open GitHub issues) at session start,
   before any task: cards claimed by others, cards covering the work you were
   about to do, and open needs-attention items. Never duplicate or race a
   claimed card.
2. **Claim.** Claim an `unassigned` card by moving it to `assigned` and setting
   Owner to your agent name + date. The moment ANY work exists — a first edit,
   a diff, a branch, a run in flight — the card moves to `in_progress`. Label
   it before the code, never after; the table must never lie about reality.
3. **Work with the card current.** Update status and Evidence as you go.
   Stuck or awaiting review? Move to `needs_attention` with a tagged note
   (`needs:` blocked-on / `review:` evidence / `decision:` the question).
   Anything discovered but not delivered spawns its own card BEFORE your card
   closes.
4. **Close with proof.** `done` requires: the package suite(s) for the touched
   packages green, `git status` clean for the card's scope, Evidence naming
   the commit(s), and — for synced cards — the GitHub issue closed in the same
   commit. Then move the card to the Archive. An agent is NOT done until its
   card says so.

## Lanes

| Lane | Meaning |
|---|---|
| `unassigned` | Queued and unclaimed — free for the next available agent or team to claim. Owner must be `unclaimed`. |
| `assigned` | Claimed (or queued with an owner), nothing underway — no draft, no diff, no branch. |
| `in_progress` | ANY work exists and an owner holds it. ONE owner per card. |
| `needs_attention` | Blocked, awaiting review, or awaiting a decision — the Evidence note says which (`needs:` / `review:` / `decision:`). |
| `done` | Finished, verified, evidenced — moved to the Archive below. Never deleted; reopen by moving back to `unassigned` with a dated note. |

## Open cards

| Card | Status | Task | Owner | Issue | Evidence |
|---|---|---|---|---|---|---|
| DMR-059 | `in_progress` | Modal doc-pipeline job queue for the mailroom document processing pipeline, modeled on the Modal docs tutorial `09_job_queues/doc_ocr_jobs.py`: new `deploy/modal_doc_jobs.py` (`sandbox-doc-jobs` app — CPU-side doc queue, LLM = the deployed `sandbox-vllm` endpoint; `sandbox-doc-inbox` Volume for bytes, `extract_text` + `process_document` stages over the vendored family, `sandbox-doc-state` Dict progress, `modal run` local-entrypoint smoke, `Function.from_name(...).spawn(...)` consumer seam), `tests/test_modal_doc_jobs.py` (stubbed modal, like `test_modal_vllm.py`), `docs/modal-doc-jobs.md` (plan written), knob rows in `config/.env.example`, mock smoke → live vLLM smoke → optional CLI wiring. Standalone repo also installs the monorepo's `vllm-specialist` + `modal-specialist` opencode agents (sandbox-adapted). | opencode (general) | — | spawned at close of DMR-058 2026-09-11 (no issue — direct package work; plan written, Phase A implementation pending human answers on extraction depth / graph scope / CLI shape). Plan doc restored 2026-09-14 (Phase 8) at `docs/modal-doc-jobs.md` — it was deleted in the 2026-09-11 fork sync while the board card still referenced it; §6 now carries the **decision documentation** (3 human gate rows: extraction depth / graph scope / CLI surface, with plan defaults + alternatives + rationale, no adjudication). Phase A code still pending human answers. |
| DMR-012 | `unassigned` | Gmail triage production-readiness residuals: live re-send/provenance operator proof, CI secret-presence check (`GMAIL_ADDRESS`/`GMAIL_APP_PASSWORD`), Langfuse/Phoenix trace checklist for `gmail-triage`, ops-lamp alerting on `checks.gmail_intake`, and watcher-stability/free-swarm/failed-bin runbooks. HUB-037/039/043/048 deliveries are in place — do not redo. | unclaimed | [#17](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/17) | synced from HUB-064 [#43](https://github.com/Exios66/mailroom-dev/issues/43) |
| DMR-013 | `unassigned` | Run the live `packages/The-Mailroom/scripts/publish_pages.sh` so the terminal site goes live at the Pages root (currently the v0.3.0 pixel console is served; `/terminal/` 404s) and verify `publish_pages.sh --status`; TUI/site code, 0.4.0 release, and upstream sync are already delivered. | unclaimed | [#18](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/18) | synced from HUB-054 [#25](https://github.com/Exios66/mailroom-dev/issues/25); only the live publish is pending |
| DMR-014 | `unassigned` | Specialist mutations on mailroom-corpus v7+v8: **fitness/registry + first evolution pass** — the structured mutation operators ALREADY ship in `packages/llm-entity-extraction/scripts/prompt_engineer.py` (`llm_propose`/`validate`/`apply_mutation`; additive-only rule mutations, one rule per iteration) with `config/prompt_engineer/agents.yaml` as a mutation registry (sorter_docclass_pilot, judge_correctness_docclass_pilot, contracts_specialist). What remains: a fitness function / same-surface A/B loop over the v7+v8 corpus rows (the "first evolution pass") plus tokenizer-budget alignment (GT-integrity half now evidenced by the HUB-032 reconciliation + `content_sha256` loader proof). | unclaimed | [#19](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/19) | synced from HUB-062 [#11](https://github.com/Exios66/mailroom-dev/issues/11); existing surface re-audited 2026-09-14 (mailroom-issues#50) — operators + registry ship, the evolution pass does not |
| DMR-017 | `unassigned` | GEPA prompt mutations on docclass v7+v8: run the prompt-engineer/GEPA loop against the extended corpus surface and register further `mailroom_v` docclass lineage keys (sorter + judge/reviewer) with same-surface A/Bs. `needs:` the DMR-046 grading-language decision; never mutate frozen `docclass_*` keys. | unclaimed | [#22](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/22) | synced from HUB-061 [#7](https://github.com/Exios66/mailroom-dev/issues/7); DMR-015's `mailroom_prompts` lineage (16 keys) is registered — A/B runs still pending; DMR-036 confirmed 2026-09-10 |
| DMR-018 | `unassigned` | v9 corpus candidate: re-evaluate INSURBIAS (CC-BY-4.0 insurance-claim narratives) as a v9 source alongside the P4 expansion priorities — insurance workflow is now MEDIUM with adjuster 150/950 after v8 — license permitting and only with decision/adjudication GT or an explicit GT-gap convention. | unclaimed | [#23](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/23) | synced from HUB-036 [#27](https://github.com/Exios66/mailroom-dev/issues/27); v9 (mailroom-dataset, 3,302 rows, pin 46a4d3c2) PUBLISHED 2026-09-12 (docs/wiki/HF-Corpus.md) — card text pre-dates it; INSURBIAS evaluation decision remains pending; DMR-037 confirmed 2026-09-10 |
| DMR-046 | `needs_attention` | Docclass grading-language decision per plan §67: add five-class + `unknown` grading language to the docclass role prompts (judge/reviewer/arbiter/boss) or keep the extended 8-class surface as deliberate eval design (KANBAN-033 lineage); extended-8 grading persists at `src/prompts_docclass.py:92-110,246-250,313-315,371-372`. `decision:` keep the extended 8-class surface as deliberate eval design (KANBAN-033 lineage) or move role-prompt grading language to five-class + `unknown` — human adjudication required, needs_attention until decided. | unclaimed | [#49](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/49) | spawned from DMR-016 residual at close 2026-09-10; DMR-017's A/B surface is fixed by this decision. **Evidence audit 2026-09-14 (Phase 8, no adjudication):** (1) The extended-8 grading is the canonical lineage — `_DOCCONTEXT{,_V1,_V2}` all grade `contract, corporate_record, due_diligence, correspondence, compliance_filing, court_opinion, insurance_claim, merger_agreement`; the PILOT 5-class set (`contract, corporate_record, correspondence, insurance_claim, merger_agreement`) is a separate `.replace()`-derived surface for the pilot-140 eval only, never the live role prompts. (2) The live corpus v9 GT (mailroom-dataset @46a4d3c2, 3,302 rows) carries exactly those 8 classes — no doc row requires the PILOT-only 5-class reduction, so five-class + `unknown` would grade a surface narrower than ground truth. (3) `unknown` is an explicitly REJECTED token in the current role prompts (prompts_docclass.py:510,611,725,737,749,761 — "`unknown` is not a valid token"), so adding `unknown` grading language would reverse existing doctrine. **Recommendation (evidence-based, not binding):** keep the extended-8 surface as deliberate eval design (KANBAN-033 lineage); the PILOT 5-class surface already covers any reduced-class evaluation need, and `unknown` grading would fight the corpus's actual 8-class distribution. Awaiting human confirmation. |
| DMR-050 | `unassigned` | Publisher GT coverage backfill via corpus-eda: correspondence `subject_matter`/`keywords` (0/40 test), corporate_record test row empty, insurance `adjuster` (16/96) — labeler over the test split, re-publish via `intent_backfill`/`docclass_uploader`, bump both corpus pins together. | unclaimed | [#53](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/53) | lucius 2026-09-10 (live Hub read at pin `eafe1ab4`); routed to the publisher by the DMR-044..052 sweep close 2026-09-10 |
| DMR-063 | `in_progress` | Full **300-doc live run** through the Modal-deployed vLLM endpoint (`https://exios66--sandbox-vllm-serve.modal.run/v1`, Qwen/Qwen3-8B L4, v0.29.0) after the DMR-062 sorter pilot (7/7, F1=1.0): extend `config/runs/pilot-sorter-modal-hf.yaml` → `config/runs/run-300-modal-hf.yaml` (dataset provider `huggingface` — `Lucius-Morningstar/mailroom-dataset` pinned `46a4d3c2`, config `ground_truth`, strata `[insurance_claim, contract, merger_agreement]`, limit 300) or the DMR-059 doc-pipeline queue once Phase A ships; `sandbox run preflight --live` → `start --watch --job-mode endpoint`, concurrency 4-8, trace sink none (or langfuse if compose is up); THEN bump the family pins downstream (llm-mailroom/entity `deploy/modal_vllm.py` + docs) and propagate the sandbox delta to Exios66 + fork via the DMR-054 patch-push. `gates:` DMR-066 ✅ (delivered 2026-09-16, `strata.field` subclass stratification + loud guards, sandbox `36ec8cb`), DMR-067 ✅ (delivered 2026-09-16, sqlalchemy restored, import-verified), DMR-068 🟡 (protocol ready `docs/scale-matrix.md`, GPU cells pending spend approval; AWQ decision gates the engine default). The 300-doc spec should use `strata.field: expected_subclass` (see `config/runs/run-50-subclass.yaml` for the working 13-stratum form). | opencode (general) | — | spawned at close of DMR-062 2026-09-16 with the pilot spec + verified endpoint; pilot evidence in commit `d586dac`. **50-doc scale-out milestone DELIVERED 2026-09-16** (`run-50-modal-hf`, done 50/50, 49/50 = 98.0% accuracy, 0 errors, 0 timeouts; commits `4dfe13c` taxonomy 300s + spec + teardown, `1ea93bc` report + headless teardown; full breakdown `reports/RUN50-MODAL-HF-REPORT.md`): per-item mean 971s/med 883s/p90 1520s, ≈0.65 docs/min aggregate on 2-3 warm L4s (max_containers 4, concurrency 16, scaledown 600s); engine gen 8.7-13.9 tok/s/L4, prefix-cache 56-58%, KV ≤30%; Modal metered $7.98 → credited → billed $0.00; resume proven (orphan driver loss at cursor 31 → `sandbox run resume` → done); teardown verified zero containers via `deploy/teardown_vllm.sh`. **Strata reality**: prepared rows are contract-class × 19 subclasses — `expected_doc_class` is constant in `ground_truth`, so the strata filter cannot stratify (see DMR-066). |
| DMR-069 | `needs_attention` | **Scale-matrix GPU cell execution** (spawned at DMR-068 close 2026-09-16): execute the fixed-doc-size scale matrix per `docs/scale-matrix.md` — fixture build step first (2k/6k/12k buckets from run-50 rows), then the 7-cell matrix {1,2,4}×L4 × conc {8,16,32} + AWQ cell using exemplar specs `config/runs/scale-c1-4xl4-c16.yaml` (bf16) and `config/runs/scale-d1-awq-4xl4-c16.yaml` (quant-only delta); fleet pinning `MODAL_VLLM_MIN_CONTAINERS=4`; measurement contract = items.jsonl primary + engine `/metrics` + log windows + billing per cell; teardown per cell via `deploy/teardown_vllm.sh`; promote AWQ to the engine default only with accuracy ≥98 % on the gate. `decision:` approve the Modal spend for the GPU-cell matrix. **HUMAN CALL (spend approval)**: est. **$13.08 ≤ $15** on L4 at $0.80/GPU-hr; last run was metered $7.98 → credited → billed $0.00 — confirm spend before GPU cell runs start. | unclaimed | — | spawned at DMR-068 close 2026-09-16 with the full protocol verified in-tree (`docs/scale-matrix.md` 7-cell matrix lines 50-58, AWQ ≥98 % gate lines 21-30/78-85, measurement contract 64-76, exemplar specs byte-identical except quantization; `MODAL_VLLM_MIN_CONTAINERS` wired at `deploy/modal_vllm.py:68,:231` + `deploy/README.md:109` + `config/.env.example:67`) — GPU-cell artifacts (fixtures/reports/run dirs) are genuinely absent, so this card owns the execution, not DMR-068. |
| DMR-075 | `unassigned` | Refresh the served-board Owner dropdown's **Agent roles** optgroup (`board-site/index.html`): it still hardcodes the pre-DMR-062 11-entry roster (`athena-database-agent`, `lucius`, `atom`, `prompt-engineer`, `hazel-ui-software-master`, `jarvis-systems-maximizer`, `vllm-specialist`, `modal-specialist`, `board-evidence-auditor`, `explore`, `general`); AGENTS.md's roster has since grown to 15 entries (DMR-062 added `archivist-file-organizer`, `code-analyst`, `test-suite-auditor`, `orchestrator-governor`) — add the 4 missing entries (see the live dropdown at `board-site/index.html` Owner edit modal; `docs/wiki/Served-Board.md` documents the intended shape). | unclaimed | — | spawned from the hub#74 docs/wiki drift sweep 2026-09-21 (mailroom-issues#74): the sweep fixed the wiki roster counts (Home.md, Served-Board.md) to AGENTS.md's 15-entry truth, but `board-site/` is outside that card's boundary — the dropdown itself still serves 11; refresh + redeploy (`vercel` or Git-integration push to `main`) closes this card |
| DMR-076 | `in_progress` | Operator-desk production hardening per issue #77 (**implement** — the issue's 16-file delta exists in NEITHER the monorepo NOR `Exios66/The-Mailroom`, both verified pre-fix): fail-fast secrets (`MAILROOM_OPERATOR_JWT_SECRET` / `MAILROOM_OPERATOR_ADMIN_PASSWORD` as `${VAR:?}`, no dev defaults), nginx `:80` sole published front door (remove backend 8001 publish, remove bogus `try_files` on proxy_pass, keep `/api/` `/v1/` `/ws` `/ws/pipeline`), delete `mailroom-ui` sidecar service, add `operator` + `ui-builder` Dockerfile stages with default target byte-identical to the hosted build, new `ui/.dockerignore`, doc currency (README, `operator_desk/README.md`, `docs/operator-desk.md`, `wiki/Operator-Desk.md`, CHANGELOG, `.env.example`, `scripts/setup_operator.sh`, `.cursor/skills/operator-desk/SKILL.md`, `ui/README.md`) in the same commit; sync unit = `sync_packages.py push --package The-Mailroom --patch --verify-suite`; no Node toolchain in the runtime path; `ui/` stays optional and documented not-in-production. | Devin (dispatch) / hazel-ui-software-master (impl) | [#77](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/77) | claimed 2026-09-22T14:52:30Z by Devin via orchestrator-governor dispatched plan (units: explore seam-map → hazel-ui-software-master impl spec → test-suite-auditor gates → caller ships → general sync → board-evidence-auditor close-out). Gates: `pytest tests/ -q` (354 expected), `docker compose config` ± secrets (unset → exit 1), live `docker compose up --build` smoke (`/desk` login, `/api/health`, `/ws`), `nginx -t` via `nginx:alpine`. **Issue #77 label-sync + close DEFERRED — no GITHUB_TOKEN on host**; board-only until token available. |

## Rules that keep the board honest

- **One owner per card.** Never work a card someone else owns — offer help on
  the issue or take over by an explicit handoff recorded in Evidence.
- **Claim before edit.** A card you are about to touch is claimed by you in
  the same session the work starts; an unclaimed card is fair game but must
  be claimed at its first edit.
- **Update, don't duplicate.** Work that addresses an existing card's problem
  updates THAT card — never a parallel card, never a duplicate issue.
- **Later timestamp wins.** If two edits collide on one card, the later dated
  note stands; the overwritten party posts a correction rather than reverting.
- **No silent completion.** A card without its closing Evidence is, to every
  other agent, still in flight.
- **Commit discipline.** Reference cards in commits: `DMR-00N: <summary>` or
  `DMR-00N claimed/reopened` in the message body. **DMR prefix is the law
  (mailroom-issues directive 2026-09-14):** any commit touching the board —
  this file, `scripts/board_state.py` + board tooling, `.github/` board
  templates/workflows, `board-site/` — MUST lead with the `DMR-0NN:` card it
  affects. `HUB-0NN` prefixes belong to the predecessor `Exios66/mailroom-dev`
  board and are never used for NEW board work (DMR-001 lineage law); they may
  appear only as historical notes in message bodies. **Stage targeted paths
  only:** `git add <explicit paths>` for exactly your card's files — never
  `git add .` / `-A` / a bare directory. This is a shared checkout: before
  every commit, `git status --porcelain` and unstage anything you don't own.

## Issues vs board — the card↔issue law

Every board card has a **synced GitHub issue** in this repo — one card = one
issue, opened from the *Board card (DMR-0NN)* template
(`.github/ISSUE_TEMPLATE/hub_card.yml`). Why this is a law: the served board
at https://digital-mailroom-theta.vercel.app reads `kanban`-labeled issues, so
a card without a synced issue never appears on it. Concretely:

- Open the card's issue before (or while) claiming it; fill the card's
  Issue column with the full link. Apply the `kanban` + `stage/*` +
  `priority/*` + `domain/*` labels (`board_state.py sync-issues` does the
  label mapping from the board).
- **Mirror lane moves both ways** — board → issue: post a dated "Board lane
  move" comment; issue → board (a move made on the served site): run
  `python scripts/board_state.py pull-issues` to report the drift, then
  `--apply` to rewrite the Lane cell + append a dated Evidence note.
- **Close the issue in the SAME commit that archives the card.** A `done`
  card with an open issue (or an open card with a closed one) is a board
  inconsistency — fix it immediately.

**Board-only exemption (documented per mailroom-issues#50):** direct package
work that never touches hub governance may carry `—` in the Issue column and
no synced issue (live cards DMR-057/058/059 follow this pattern, with the
Evidence note "no issue — direct package work"). The cost is visibility: the
card does not appear on the served board until synced. The exemption is
**only** for package-scoped work with no hub decision needed; any card that
spans packages, touches the org, or needs a shared decision gets a synced
issue.



## Archive

- **DMR-074** (done 2026-09-16) — **Agent instruction files point agents through the sync & release flow** — Owner `opencode (deepseek-v4-flash)` — DELIVERED (card commit `0135382a` 24 files/640 insertions, cursor re-baseline `d63a950a`; claim + issue [#69](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/69) opened first): every agent instruction file now carries the distinct file & package management flow. **(a) Root `AGENTS.md`** gained the **Sync & release at a glance** section (before Commands): the three moving surfaces (monorepo dev source-of-truth → `Exios66/*` standalone mirrors → `vendor/` snapshots), the five commands every agent must know (`sync_packages.py status|push --package --patch|push --all --patch`, `sync_vendor.py`, `release_chain.py cut X.Y.Z --apply --tag`, `release_notes.py X.Y.Z`), the two never-conflated release paths (hub chain vs standalone package release + propagate), and the DMR-074 ownership note (planning/dispatch = `orchestrator-governor`, execution = `general` missions, never hand-edit a mirror). **(b) 13 code-shipping specialist files** in `.opencode/agents/` gained one **byte-identical "Package & release law (DMR-074)" tail block** (md5 `92c82e8e…` verified across all 13): monorepo-source-of-truth law, `sync_packages.py` propagation (push-leg decision tree + `--verify-suite`), `sync_vendor.py` obligation, both release paths + release-time pin law (`bump_dojo_scoring.py`), and "a deliverable that must reach a standalone repo is a sync unit on the card — chain `orchestrator-governor`/`general`, never hand-edit the mirror". `orchestrator-governor.md` additionally gained a **Sync & release train** roster row, a sync/release unit-of-work bullet in its card lifecycle, and a startup-ritual pointer to `docs/wiki/Sub-Package-Sync.md` + `Releases.md` + `docs/TESTING.md`. **(c) Global reconcile** — `~/.config/opencode/agents/*.md` overwritten byte-identical to the project mirrors for 6 stale files (athena, atom, hazel, jarvis, lucius, prompt-engineer — the global copies were pre-constellation generic descriptions) and the 4 missing global-team files added (archivist, code-analyst, test-suite-auditor, orchestrator-governor); project-local specialists (vllm, modal, board-evidence-auditor) stay project-only per doctrine; `docker-deployment-specialist` left as-is; `PROMPT_ENGINEER_GEPA_PROVENANCE.md` verified identical. **(d) 8 package surfaces** gained their monorepo sync contract: llm-entity-extraction/AGENTS.md (Monorepo development section + release step 10 propagate), The-Mailroom/AGENTS.md (Monorepo development section + propagate-after-release), claims-data-eda + Enron-Evaluation-Environment (Monorepo home note — virtual members), mailroom-corpus-eda/AGENTS.md (one-liner expanded), llm-dojo-scoring/README.md (Releases & monorepo sync + pin law + tier-3 gate), agent-mailroom/README.md (Monorepo & sync pointer), local-mailroom-sandbox/AGENTS.md (family-flow pointer). **(e) docs/wiki/Releases.md** gained the Ownership section. **Sync unit on the card (propagated):** `sync_packages.py push --all --patch` landed the 8 package deltas upstream with DMR-073 verified landings — Enron `160d9077`, The-Mailroom `cf2c5998`, agent-mailroom `189cc9f1`, claims-data-eda `a3fbca3c`, llm-dojo-scoring `90cc129a`, llm-entity-extraction `42c54929`, sandbox `f51f8f44`, Mailroom-Corpus-EDA `9b2b9409`; llm-mailroom + llm-mailroom-graph no-delta; cursor re-baselined (`d63a950a`). **Gates:** `board_state.py check` 0/0 (11 open cards), `audit_references.py` clean, `release_chain.py check` OK, `github_labels.py audit` in sync, 13 tail blocks byte-identical, global↔project byte-identity verified, `git status` clean for the card scope. Docs-only card — no package suites required (docs/TESTING.md tier 1). Issue [#69](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/69) closed in this archive commit.
- **DMR-073** (done 2026-09-16) — **Sync tooling: kill the silent no-op push class (DMR-071 propagation hole)** — Owner `orchestrator-governor` — DELIVERED (commit `e07d6f04`; claim `dd6be5b1`): the DMR-071 audit's observed false-success signature is structurally impossible now. (a) `push --patch` proves every landing with a post-push `ls-remote` probe that must equal the pushed worktree commit — unchanged tip → exit 5 `verify` 'SILENT NO-OP GUARD', unreachable remote → exit 1, foreign concurrent tip → exit 5; the success line names the POST-push tip + pushed commit (never the stale pre-push probe); success records carry `pushed_commit`/`landed_tip`. (b) The empty-delta path cross-checks its claim via `local_ahead_paths` — 'empty diff while the monorepo is ahead' is an exit 5 `extraction mismatch` refusal, not a trusted no-delta. (c) The subtree leg reports the true post-push tip and says plainly when the remote did not move (git-verified already-contained). (d) Cursors re-baseline only after a verified landing. (e) Hermetic pins: `TestSilentNoOpGuard` — 9 unit tests covering every guard branch on both push legs; scripts/tests **44/44 green**. **Live validation:** `push --all --patch --dry-run` — all 10 packages traversed the new cross-checked no-delta path against the real remotes (every 'no content delta' now proven, not assumed). Docs truth: AGENTS.md sync-failure law + CHANGELOG entry in the same commit. Board hygiene ride-along: removed the stray duplicate DMR-069 table row that sat inside the Archive section (commit `dd6be5b1`). Issue: none — board-only direct tooling work (monorepo scripts/ never propagates). 
- **DMR-072** (done 2026-09-16) — **llm-mailroom synthetic compliance sample residue: gate-test reconciliation** — Owner `orchestrator-governor` — DELIVERED (commit `dmr072`): `src/scripts/prepare_samples.py` verified already compliance-free (DMR-057 removed generation); the manifest itself (`docs/examples/samples/manifest.csv`) does not exist in EITHER the monorepo or upstream (pruned heavy asset — the gate test is universally skipped via the documented monorepo-side skipif); the residue was the gate test's stale expectations + stale prose. Fixed: `test_real_sample_gate.py` synthetic set 10 → 8 (compliance_01/02 out; 5 live-class stand-ins + 3 insurance letters), mock count 25 → 23, retired-id negative guards retained; README 25-sample → 23; AGENTS.md 3 spots (mock line, pilot-samples roster, Langfuse dataset summary 13 → 11). **Proven** (not just skipped): expectations validated against the live `is_real_sample`/`filter_real_samples` source logic with a reconstructed 23-row manifest — including the property that re-added compliance ids stay blocked in `--real` mode. Full llm-mailroom suite 1053p/36s. 
- **DMR-071** (done 2026-09-16) — **Correspondence subclass parity: the `voicemail` token decision (HUB-041 law) — ADJUDICATED + DELIVERED: all subclass taxonomies re-pinned to the current dataset** — Owner `orchestrator-governor` — **ADJUDICATION (human, 2026-09-16):** the most recent mailroom-dataset pin (v9, tip `46a4d3c2`, 3,302 rows, GT-closure 2026-09-13) is the source of truth for ALL subclass taxonomies, all five classes + all mapped subclass strata. **Delivered (this session):** the authoritative v9 GT vocabulary extracted live from the Hub `ground_truth` parquet (expected x expected_subclass, 3,302 rows); `voicemail` confirmed ABSENT from v9 correspondence GT → removed from every surface (entity sorter catalog + 2 prompt strings + test pin, dojo `DOC_TYPE_SUBCLASSES` + `CORPUS_SUBCLASS_SURFACES` + test pin, 28 docclass-arm prompt strings in The-Mailroom `docclass_prompts.py`, docs/wiki/HF-Corpus.md table); `corporate_record` re-pinned to the 10-token v9 observed set (parity gate `HUB_SUBCLASSES`, dojo observed-GT surfaces + `HUB_SUBCLASS_INVENTORIES`, The-Mailroom `DOC_SUBCLASS_BY_CLASS`) with `certificate_of_formation` as the sole documented scoring-enum extra; contract (26 folder spellings → 25 CUAD keys), merger_agreement, insurance_claim verified already aligned. `scripts/taxonomy_parity.py` **exits 0** (first green since the drift surfaced). **Gates:** llm-dojo-scoring 371p/5s; llm-entity-extraction 774p/28s; The-Mailroom 336p; llm-mailroom (dependent consumer) 1053p/36s. Enron eval-env labeler keeps its voicemail key locally (0% by construction in text-only corpora) — documented intentional divergence. Out-of-scope residue spawned: DMR-072 (llm-mailroom synthetic sample-gate ids). 
- **DMR-070** (done 2026-09-16) — **Sync tooling lessons from the v0.6.0 experience → ENFORCED TOOLING: post-import verification seams (deletion guard, divergence audit, --verify-suite)** — Owner `orchestrator-governor` — DELIVERED (commits `d8e20d01` tooling+docs, `7347f62a` vendor/retirement, `95e64880` board dedup, `b090833d` release cut): (a) `pull --open-pr` blob-compares every ladder-resolved conflict path against BOTH merge sides — a resolution matching neither (the v0.6 `corpus.py` else-branch mangle class) is reported loudly + carried as `resolution_divergences` + appended to the PR body (proven live: PR [#66](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/pull/66) recorded `resolution_divergences: []`); (b) `push --patch` REFUSES deletion-bearing packages (exit 5, error_class `verify`, `deleted_paths`) BEFORE the containment guard — killing the misleading 'pull first' doom-loop remediation; (c) `--verify-suite` on pull/push runs the touched package's suite (`SYNC_VERIFY_CMD` override) before any push/cursor advance; exit code 5 'verification refused' added (severity 5>4>3>2>1>0). Docs truth: AGENTS.md Sub-package sync + Sub-Package-Sync wiki carry the push-leg decision tree (content-only vs deletion-bearing), the vendor workspace doctrine, and the tag-refresh resurrection warning. Hermetic pins: scripts/tests 35/35 green (14 new). **Live validation of the decision tree:** the sandbox propagation hit BOTH documented cases — subtree push non-fast-forward (patch-push tip not an ancestor) → grafted via `pull --open-pr --resolve ours` (PR #66, true merge) → full subtree push carried the deletion-bearing delta (Exios66 tip `ef77cec4`, cursor re-baselined; fork `LLM-Mailroom-Services/mailroom-sandbox` pushed to the same tip); llm-mailroom's content-only delta traveled the patch leg. Issue: none — direct tooling work (board-only exemption). 
- **DMR-057** (done 2026-09-16) — **Self-containment: vendor snapshots track the WORKSPACE (hub#62 doctrine) — refresh tooling landed, docclass-era residue purged, retirement completed, delta propagated** — Owner `orchestrator-governor` — DELIVERED (commit `7347f62a`; release v0.7.0 `b090833d`): new `scripts/sync_vendor.py` mirrors workspace → vendor carrying BOTH content AND deletions (+`--check` CI gate; hermetic pins in `scripts/tests/test_sync_vendor.py`); `sandbox fetch-deps` reworked workspace-mirror-first (tag fallback loud — the v0.7.1 tag predates 59c47401, a tag refresh resurrected nothing ever again); vendored llm-mailroom refreshed to the post-removal workspace (26 files, 5 docclass-era files DELETED incl. `agents/compliance_specialist.py` — closes the DMR-066-flagged `test_vendor` pin-mismatch + `test_vendor_drift` `[llm-mailroom]` compliance-residue set); compliance retirement completed across the sandbox surface (eval registry, prompt roster, components.yaml `retired_agents`, taxonomy overlay + base `compliance_filing` class removed) + the guard-invisible `agents/README.md` roster prose fixed in the workspace and mirrored; both VENDOR.md files + sandbox AGENTS.md/README.md + test_vendor.py moved to the workspace-snapshot doctrine. **Verified:** sandbox suite 286p/1s (full run); sync_vendor --check 0 drift both trees; compliance guard OK; board check 0/0; taxonomy_parity pre-existing `voicemail` flag → spawned DMR-071; llm-mailroom sample-gate residue → spawned DMR-072; 10/10 repos in sync, 0 monorepo-ahead; propagated Exios66 (`ef77cec4`) + fork (`ef77cec4`) + llm-mailroom (patch). 
- **DMR-064** (done 2026-09-16) — **Sync tooling hardening — error handling, merge-conflict handling, and PR-based merge flow (DMR-064, 2026-09-16) — tooling SHIPPED and the v0.6 sync + release mission it was created for COMPLETED** — Owner `orchestrator-governor` — DELIVERED: tooling commits `2ff6f1f8` (conflict detection/classification + SAFE ABORT, `pull --open-pr [--resolve ours|theirs]` PR branch flow, exit-code taxonomy 0/1/2/3/4, `--json` parity, entry-guard recovery, patch_push leak hardening, atomic manifests, per-package cursor saves, 11-test hermetic harness), `c74fe87c` (AGENTS.md contract + CHANGELOG), `56b30e9a` (10 audit must-fix pins, 21 tests); compliance-removal cementing `879f89c0` + guard test `scripts/tests/test_compliance_removal_guard.py` (all 8 forbidden docclass-arm paths assert-absent, zero tokens in the llm-mailroom src/docs surface, taxonomy = 5 classes + unknown). **MISSION CLOSE (v0.6 sync + cut, 2026-09-16):** release-train sweep `push --all --patch` landed The-Mailroom (`d9a72083`), llm-dojo-scoring (`34b4da43`), local-mailroom-sandbox (`a772c5e2`, 43-file delta) — commit `18ba15b7`; llm-mailroom went through the DMR-064 branch flow end-to-end: `pull --open-pr --resolve ours` opened PR [#65](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/pull/65) (import `d62a169f` with ours resolution; the `-X ours` auto-merge had resurrected `skills/compliance_specialist/README.md` — re-removed in `cae2570d` so all 8 forbidden paths verified absent, guard green), PR merged `13daa79c`, full `git subtree push` carried the 8-path compliance/docclass deletion upstream (tip `1a6bd401`, remote-verified absent), cursor re-baselined `e8db701a`. **Result: 10/10 standalone repos in sync, 0 monorepo-ahead, 0 cursor gaps; scripts suite 22/22 green; compliance specialist REMOVED + retired on `Exios66/llm-mailroom`.** Hub release **v0.6.0 cut** (`c4a4b7a1`, annotated tag `mailroom-hub v0.6.0`, release_chain check OK, GitHub Release live) — sync-tooling hardening + compliance removal + sandbox self-containment are the release's headline features. Board warnings on DMR-066/067 (done-not-archived) + DMR-068 (in-progress-unclaimed) pre-date this close and belong to the DMR-063 sandbox mission, not this card.
- **DMR-068** (done 2026-09-16) — **Scale-out flatness at 4×L4 — protocol delivered; execution spawned as DMR-069** — Owner `unclaimed` — **PROTOCOL DELIVERED + VERIFIED at closure audit 2026-09-16**: `docs/scale-matrix.md` 7-cell matrix {1,2,4}×L4 × conc {8,16,32} + AWQ cell (lines 50-58), AWQ lever w/ ≥98 % accuracy gate (21-30, 78-85), measurement contract (64-76), est. **$13.08 ≤ $15** (L4 $0.80/GPU-hr); exemplar specs `scale-c1-4xl4-c16.yaml` (bf16) + `scale-d1-awq-4xl4-c16.yaml` (quant-only delta, byte-identical otherwise); `MODAL_VLLM_MIN_CONTAINERS` fleet pin wired (`deploy/modal_vllm.py:68,:231`, `deploy/README.md:109`, `config/.env.example:67`); teardown per cell via `deploy/teardown_vllm.sh`. GPU-cell artifacts genuinely absent (no fixtures/reports/run dirs) — honest remaining work **spawned as DMR-069** (fixture build + GPU cell runs + spend approval) before this close. Engine log windows (gen 8.7-13.9 tok/s, prefix 56-58%, KV ≤30%) remain the scale-matrix evidence base in this archive.
- **DMR-067** (done 2026-09-16) — **Add `sqlalchemy` to the sandbox `[pipeline]` extra** — Owner `athena-database-agent` — CONFIRMED at closure audit 2026-09-16: `packages/local-mailroom-sandbox/pyproject.toml:53` carries `"sqlalchemy>=2.0"` in `[pipeline]` (with DMR-067 comment; CHANGELOG.md:84-90); vendored `vendor/llm-mailroom/src/storage/catalog.py:3-4` + `storage/audit_log.py:3-4` import sqlalchemy; `src/llm/prompts.py` exists; live import check passed (`from storage import catalog, audit_log; from llm import prompts` clean, venv sqlalchemy 2.0.52 satisfies `>=2.0` — the card's 2.0.54 was the sandbox-side install version, spec is correct). The one live sorter row is deferred by design to the DMR-063 300-run preflight step (import-level evidence complete).
- **DMR-066** (done 2026-09-16) — **Fix sorter eval stratification — closed with a corrected suite claim** — Owner `unclaimed` — implementation delivered via sandbox `36ec8cb`: `strata.field`/`values`/`counts` subclass-stratified draws w/ class-aware normalized matching + `other`-fallback guard, loud preflight guards (absent value / constant field w/ >1 request / draw-dropped stratum → exit 1), lock records `strata_actual`; `config/runs/run-50-subclass.yaml` locked at 50 rows / 13 strata (Service 8 … Manufacturing 2). **CLOSURE AUDIT 2026-09-16 found a merge regression**: the import `371d42ff` had replaced upstream `else: chosen = rows` with a legacy bucket loop dereferencing absent `strata['buckets']` → KeyError on every plain draw (suite 254p/32f, NOT the claimed 271p/3s) and the v0.6 sweep had propagated the broken blob upstream (tip `a772c5e2`). **FIXED `57cdecf8`**: else-branch restored verbatim from upstream blob `36ec8cbdae52` (byte-verified); post-fix suite 284p/2f/1s — the 2 remaining failures are the pre-existing DMR-057 vendor-drift set (test_vendor pin mismatch + test_vendor_drift `[llm-mailroom]` compliance residue), tracked by DMR-057; fix propagated upstream via patch push (tip `6ac17418`, cursor re-baselined `227dbd9b`). Closeable: yes — implementation + regression fix + upstream healed.
- **DMR-065** (done 2026-09-16) — **(renumbered from the colliding DMR-062 sandbox-board id 2026-09-16 by DMR-064 governance** — the monorepo's DMR-062 is the Agent roster card archived 2026-09-14; this entry carries the sandbox board's HF-secret card number, history preserved in git. Renumber note added 2026-09-16: duplicate-card-id structural repair) — **Modal HF-secret wiring + v0.29.0 live parity pilot — deployment fixed, endpoint verified, sorter pilot green** — Owner `orchestrator-governor` — DELIVERED (commit `d586dac`, `Exios66/local-mailroom-sandbox`): the newly configured Modal secret `huggingface-secret` is now attached to `serve` + `download_model` via `Secret.from_name(..., required_keys=["HF_TOKEN"])` (`MODAL_HF_SECRET_NAME` override); `HF_TOKEN` dropped from the local-env knob secret (Modal secrets apply list-order, last wins — a local HF_TOKEN would have silently overridden the named secret). Container boot log on the deployed revision proves the seam (`secrets check: HF_TOKEN=set`); `/v1/models` 200 with bearer (Qwen/Qwen3-8B, max_model_len 16384), `sandbox health` ok:true, `json_object_ok:true`. **v0.29.0 parity**: Modal + compose + HTCondor pins + docs moved together (vllm-specialist docs-verified the flag set 2026-09-16: 16384 boot-valid under MRV2-default, 32768 still raises, `--no-enable-log-requests` intact). **Bug fixed**: preflight engine probe appended `/v1/models` to an already-`/v1`-seamed base (`.../v1/v1/models` → live 404) — now normalized + pinned (`test_engine_probe_url_seam_normalizes_v1_suffix`); `test_vendor` stale-pin scan no longer trips on pipeline-extra venv wheels. **Pilot**: `config/runs/pilot-sorter-modal-hf.yaml` — 7/7 rows F1=1.0 (contract 3/3, insurance_claim 3/3, merger_agreement 1/1), `mock:false`, `serving_kind=modal`, state done errors 0. **Verified:** sandbox 260p/3s; 2 specialist doc audits (modal SDK 1.5.5 secret semantics; vLLM v0.29.0 CLI) both cited current upstream docs. Follow-up parked on DMR-063 (300-doc run) + DMR-057 propagation of this delta to the fork.
- **DMR-061** (done 2026-09-14) — **Live-or-loud sweep (sandbox + monorepo) — all silent-failure sites fixed loud + test-pinned** — Owner `opencode (general)` — DELIVERED (commits `2c3481e6`, `1ef3aef8`, `9396b757`; board_state Z-date fix rides `2c3481e6`-era gate): research (6 read-only audits, all prior live-or-loud claims VERIFIED) → fixes for 12 SILENT + ~25 SEMI-LOUD sandbox clusters (tracing counters/warns, engine-base raise, cooldown fail-closed, intake provenance, corrupt-GT raises, scoring-method markers, rc discards, dead code), entity `deploy/modal_vllm.py` reconciled to DMR-056 (16384/Xet/prewarm/--check/TP), deploy surfaces (notification=Error, modal_job terminal re-raise, compose healthcheck, rc-in-log), repo-wide propagation (durability_gate fail-fast, 20 debug→warning, HF ladder raise + unpinned warns + tip-drift provenance, deploy_gh_pages exit-1, dojo emitter counters, The-Mailroom/agent-mailroom/board-site/sync-wiki/release-notes/board_state lent), 23 new pinning tests (sandbox 13 incl. `test_live_or_loud_sweep.py`, dojo 2, llm-mailroom 3, entity 5 + `test_durability_gate_guard.py`), vendor snapshots re-copied for the hub#62 byte-identical guard, CHANGELOG v0.7.1/v0.15.0 pins, dojo VENDOR.md commit hash, STATE.md entry. **Verified:** sandbox 272p/1s; entity 774p/28s; llm-mailroom 1061p/36s (docs-truth pre-existing red fixed); dojo 371p/5s; The-Mailroom 336p; agent-mailroom 86p; corpus-eda green; claims 40p; Enron 87p; `board_state check` 0/0; `audit_references` clean; sync status all-10 in sync. Follow-up parked on DMR-057: propagate the delta to `Exios66/local-mailroom-sandbox` + the fork. Issue [#61](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/61) closed in the archive commit.
- **DMR-062** (done 2026-09-14) — **Agent roster completed — 8 global specialists (4 reinstated + 4 new incl. orchestrator-governor)** — Owner `opencode (general)` — DELIVERED (commit `5275b134`): materialized the 4 phantom roster agents (`athena-database-agent`, `atom`, `hazel-ui-software-master`, `jarvis-systems-maximizer` — `opencode agent list` previously proved they did not exist) + created `archivist-file-organizer` (file mgmt/organization), `code-analyst` (concise analysis), `test-suite-auditor` (suite-quality auditing), `orchestrator-governor` (master governance orchestrator/planner, `mode: all`, PRIMARY+SUBAGENT) — canonical files at `~/.config/opencode/agents/` with byte-identical committed mirrors under `.opencode/agents/` (drift = housekeeping defect); AGENTS.md roster → 15 specialists + built-ins, new placement rule (global-first + mirror), `docs/wiki/Subagents.md` mirror table + orchestrator dispatch protocol. **Verified:** `opencode agent list` shows all 8 as (all), schema-valid. Restart opencode for the Task tool to see the new subagents. Issue [#62](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/62) closed in the archive commit.

- **hub#17 scripts sweep** (done 2026-09-14) — **Cross-package script organization/audit/pruning pass** — Owner `opencode (general)` — DELIVERED (commits `813567ee`, `305fcaf2`, `5a355e63`, `678f9da2`, `fbf1571f` — all pushed, all 10 downstream packages re-baselined **in sync**): exhaustive reference audit across every package `scripts/` dir (explore subagent: every script classified LIVE / DOCS-ONLY / DEAD against tests, skills, CI, hooks, sibling scripts). Findings: the two apparent archive candidates (`The-Mailroom/scripts/render_tui_shots.py`, `llm-entity-extraction/scripts/sync_edge_suites.py`) were LIVE in the upstream `Exios66/*` repos — monorepo-side archival reverted for tree parity (HUB-018). Real fixes landed: 4 stale scripts READMEs rewritten as accurate functional indexes (The-Mailroom demo/seed/live/export/publish/sync/release groups; Enron acquire/build/labeler-library/QA/EDA with shared-labeler consumer warnings; claims acquire/build/render/publish/QA with shared-renderer warnings; entity root-tools now indexed incl. prompt_engineer/run_agent_bench/gen_edge_cases/gt_workbench/smoke_vllm_endpoint/durability_gate); `scripts/audit_references.py` allowlist fixed for the `docs/TESTING.md` self-referential gate comment (was the last non-allowlisted hit); The-Mailroom + sandbox upstream pulls merged (independent upstream work + my v0.15.0 pin sweep reconciled — ours kept as canonical); sandbox vendor `experiment_log.md` restored for tree parity (dad8b693 exclusion is snapshot-level only). **Verified:** The-Mailroom 334p/2s, entity 764p/28s, sandbox 259p/1s; board check 0/0; audit_references clean; release_chain OK; labels in sync. taxonomy_parity remains RED (pre-existing — mailroom-issues #66/#67/#68, HUB-041 decision-gated, not part of this beat). Evidence thread: mailroom-issues#17.

- **DMR-058** (done 2026-09-10) — **Full CLI verification sweep + quickstart** — Owner `opencode (general)` — DELIVERED: exercised every `sandbox` command against a fresh in-repo `.venv` (offline + live Hub paths), published `docs/QUICKSTART.md` as the verified full command reference. Fixes landed: base deps carry the vendored dojo runtime surface (numpy/pandas/matplotlib/openpyxl — clean venv boot); `[dev]` carries the Hub client, new `[pipeline]` extra for the vendored langchain stack; compose/ollama/ssh/Hub-client/deploy-extra failures degrade to clean one-line errors (no tracebacks); `fetch-deps` layout-aware + non-destructive; `run status/resume/cancel --config` resolve the embedded `run_id`; `config/runs/example.yaml` fixture path CWD-relative again; gitignore covers mailroom runtime outputs. Verified: 230 passed / 1 skipped in the monorepo venv AND the standalone `.venv`; live `datasets pull` (sha256-verified) + full run lifecycle + legalbench suite path exercised. Propagated to `Exios66/local-mailroom-sandbox` + the `LLM-Mailroom-Services/mailroom-sandbox` fork at `7475840`. Archived per mailroom-issues#50 (was `done` in the open table with no Archive entry).
- **DMR-060** (done 2026-09-11) — **Sandbox sync-cursor re-baseline + fork `.opencode/agents` acknowledgment (Modal vLLM throughput alignment)** — Owner `opencode (general)` — DELIVERED (commits `571888e0` + `1a0083f7`): re-baselined the `local-mailroom-sandbox` sync cursor at `2cdc835` and acknowledged 2 fork-only `.opencode/agents` paths during the DMR-057 propagation sweep (Modal vLLM throughput alignment work). Card back-filled 2026-09-13 by the swarm audit (mailroom-issues#31): the two commits referenced a card that was never opened — scope folds into the DMR-057 vendoring/propagation card; board `check` was failing on the phantom reference until this entry.
- **DMR-056** (done 2026-09-10) — **Modal+vLLM CLI — three-mode readiness (mock smoke / live data / production config), debug features, eval-task extension point, docs currency** — Owner `modal-specialist (opencode)` — DELIVERED (commit `bac6382e`): four specialist audits (modal/vllm/athena/prompt-engineer) found real gaps — ALL fixed + regression-pinned. **Code:** `modal_job.py` `Dict.put` two-arg (one-arg TypeErrors on modal 1.5.5 — killed progress mirror/state dict/volume commits in a REAL deploy), `SANDBOX_DEBUG` through the deploy Secret, `download_model` directory-count guard (hf_hub 1.x returns str — old guard was dead code), `[hf_transfer]` extra dropped (Xet default), llm-mailroom app cost tags + `--check` parity; `datasets pull` is LIVE pinned (corpus loader, sha-verified, exit 1 — the silent README-marker no-op is gone); whole-run job tasks score the LOCKED dataset (Hub spec = live rows; verified: 4 real corpus rows through `task: pipeline`); `known_tasks()` spec validation; `_cmd_eval` fall-through → explicit raise; `prompts show` validates + surfaces version_key; models.yaml v0.28.0 posture corrected (RAISES, not warns — 32768→16384 defaults so the DEFAULT config boots; FP8 rows → published `-FP8` checkpoints; 70B row → `RedHatAI/Llama-3.3-70B-Instruct-FP8-dynamic`); htcondor health curl `--max-time`+bearer, `QUANTIZATION`/`REVISION` knobs, 70B example fixed. **Extension point (THE deliverable):** registering one `AgentSpec` = one-file change → auto `sandbox eval` choice + whole-run job task + matrix cell + spec validation; documented in `docs/evals.md` "Adding a new eval task" + pinned by `test_registering_new_agent_spec_is_the_extension_point`. **Three-mode verification:** (a) mock smoke fixture run done 7/7 + live-data mock run done 4/4, honest `mock:` labels; (b) `datasets pull` → 10 real rows @ `eafe1ab4` sha-verified; (c) health rc=1 honest, live-or-loud warnings, matrix validation, help matrix rc=0 ×14. **Docs (human directive — ALL sandbox docs current):** evals/jobs/SANDBOX-GUIDE/deploy README/htcondor README/README/AGENTS/wiki pages/CHANGELOG. **Verified:** sandbox 207 passed / 1 skipped (+10 tests); llm-mailroom full 986 passed / 36 skipped / 2 PRE-EXISTING failures (DMR-052-flagged, parallel agent's docclass resync — NOT this card); bash -n clean; board check 0/0. Issue [#59](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/59) closed 2026-09-10.

- **DMR-055** (done 2026-09-10) — **Docs currency sweep: reflect the DMR-044..054 delta in all documentation** — Owner `modal-specialist (opencode)` — DELIVERED (commit `e06b69f8`): root `CHANGELOG.md` `[Unreleased]` now accumulates the DMR-044..054 batch (was empty); `docs/wiki/` `Offline-Sandbox.md` (broken `--profile` switching example + tunnel order — both reproduced rc=2 — plus DMR-045/051/053 knobs + `SANDBOX_DEBUG` + 503 row), `Sandbox-Modal.md` (TP_SIZE knob, `--debug`/`--check`, `download_model` hints), `Sandbox-Jobs.md` (artifacts + lock drift fields + engine probe + diagnostics), `Home.md` (hub release v0.4.0 → v0.5.0); sandbox `README.md`/`AGENTS.md`/`deploy/README.md` (stranded table rows fixed)/`deploy/htcondor/README.md`/`docs/jobs.md`/`docs/SANDBOX-GUIDE.md`; llm-mailroom `README.md`/`deploy/README.md` (new Modal vLLM section)/`docs/configuration.md` (FREE_ONLY exemption + vLLM seam); `deploy/modal_job.py:245` guidance print `--spec` → `--config`. **Verified:** sandbox 197 passed / 1 skipped; llm-mailroom subset 101 passed; board check 0/0; wiki pushed to the live GitHub wiki (WIKI_CARD=DMR-055) and `--check` green. Issue [#58](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/58) closed 2026-09-10.

- **DMR-054** (done 2026-09-10) — **Fix `sync_packages.py` `patch_push` — ls-tree parsing + binary cat-file** — Owner `modal-specialist (opencode)` — DELIVERED (commits `9d48004b` + `9e9f9112`): the first patch push since DMR-028 crashed twice — `entry.split("\t", 1)` (2 parts) was unpacked into 4 names (`ValueError`), then `git cat-file blob` returned text-mode str that `write_bytes` rejected (`TypeError`). The ls-tree metadata/rel split now happens first (paths may contain tabs under `-z`), and `run()`/`git()` gained a `binary` flag for blob extraction. **Verified:** `push --package local-mailroom-sandbox --patch` propagated the 43-file DMR-044..053 delta to `Exios66/local-mailroom-sandbox` (remote head `8edbe4a9`, cursor re-baselined, status in sync 0 monorepo-ahead). Issue [#57](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/57) closed 2026-09-10.

- **DMR-053** (done 2026-09-10) — **vLLM+Modal script hardening — debug logs, helpful errors, diagnostics + whole-run record linking** — Owner `modal-specialist (opencode)` — DELIVERED (commit `e581f14a`): the plan-audit gap fixed (`_run_whole_run` now passes the lock's LOCAL prompt variant as `prompt_version` — delegated records used to claim `mailroom-default` despite a pinned override — and stamps the returned record with `spec_hash`/`dataset_fingerprint`/`run_id`); `run_batch_eval.sh` gains `SANDBOX_DEBUG` `set -x`, a timestamped `log()` to stderr + `results/run.log`, a once-only `fail()` diagnostics dump (package versions, masked env, dataset stats, vLLM log tail), health-wait progress logging, and an EXIT trap writing the run status; `serve_vllm.sh` echoes effective knobs + argv (auth on/off, never the key); both `modal_vllm.py` apps print a masked boot config, verify `download_model` results with gated-repo hints, and `_smoke_check` errors carry response bodies + a 401 bearer hint; `modal_job.py` wraps failures into a terminal state dict with traceback tail + runtime diagnostics and honors `SANDBOX_DEBUG`; `job/remote.py` volume failures suggest `modal token new` / `modal volume ls`; the CLI watch prints the worker traceback + diagnose hint. **Verified:** sandbox 197 passed / 1 skipped; llm-mailroom 22 passed; all three deploy apps import against real `modal==1.5.5`; `bash -n` clean; board check 0/0. Issue [#56](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/56) closed 2026-09-10.

- **DMR-044** (done 2026-09-10) — **CHTC batch eval live-or-loud — eval stack, parity flags, no silent mock** — Owner `modal-specialist (opencode)` — DELIVERED (commits `81f57b42` + `7e7827bc` + `908ea850`): the sandbox silent-mock root causes fixed (`_doc_text` NameError restored in `eval/agents.py`, prepared-row `subdir` KeyError → `doc_text` materialization in `_run_pipeline_doc`, `_predict_spec` re-raises live errors, `_live_legalbench_answer` provider-aware serve target); `run_batch_eval.sh` installs `mailroom@v0.6.0 --no-deps` (CARD TEXT CORRECTED — the original `llm-entity-extraction@v0.20.0` prescription would have shadowed mailroom's `agents/`), proves the stack importable before vLLM starts, serves with compose/Modal parity flags (32768/0.90/256 + `--no-enable-log-requests`, env-overridable, optional TP), hard-fails on health death/timeout, drops every `|| true`, passes `--model "$MODEL"`, exports `OPENROUTER_BASE_URL`, and runs a post-eval `offline_fallback` guard; results dir anchored at submission time (was transferred empty); `serve_vllm.sh` + both `.sub` files parity + 70B resource block; README §4/§5. **Verified:** `bash -n` both scripts; guard exercised both ways (exit 0 clean / exit 1 on `offline_fallback=3`); sandbox suite 190 passed / 1 skipped; static live-or-loud guard test. Issue [#47](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/47) closed 2026-09-10.
- **DMR-047** (done 2026-09-10) — **Modal job worker live-eval surface — image bundle, results return, resume/mock plumbing** — Owner `modal-specialist (opencode)` — DELIVERED (commits `0d425ad5` + `f933ff02`): the `sandbox-job` image bundles `config/` + `data/fixtures/` under `SANDBOX_ROOT=/root` plus `mailroom@v0.6.0` (whole-run tasks crashed on `load_profile`, per-item evals silently mocked); `DEFAULT_PROVIDER` joins the deploy Secret; the worker verifies the dataset sha256 against the lock, commits the Volume every 25 events, and copies the run's experiment records into the run dir; `fire()` forwards the lock's `mock` flag and skips the upload when the remote lock matches; `pull_run_dir` + CLI finalize (watch/status/resume) pull items/checkpoints/records back and append them locally; modal resume re-fires via `ensure_running`. **Verified:** all three deploy apps import against real `modal==1.5.5` (scratch venv); TP2 argv derivation; remote tests 4 new; sandbox suite 190 passed / 1 skipped. Issue [#50](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/50) closed 2026-09-10.
- **DMR-048** (done 2026-09-10) — **CLI live-or-loud wiring — health env override, pull-models branch, silent-mock warning, modal probe, matrix validation** — Owner `modal-specialist (opencode)` — DELIVERED (commit `e01bd813`): `sandbox health` follows `VLLM_BASE_URL`/`base_url_env` (and loads `.env`) so a `modal-vllm` profile probes the deployed endpoint, not localhost; `pull-models` branches by serving family (vLLM/Modal → `download_model` pre-warm guidance, never `ollama pull` of an HF id); `pilot`/`hf-pilot`/`legalbench`/`eval`/`matrix` warn on an implicit mock against a vLLM profile (live-or-loud); `run start --job-mode modal` probes `/v1/models` before firing (skips mock/`--offline`); `matrix --providers` validates real profile names. **Verified:** new `test_cli_wiring.py` (8 tests); sandbox suite 190 passed / 1 skipped. Issue [#51](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/51) closed 2026-09-10.
- **DMR-049** (done 2026-09-10) — **Eval data & metrics integrity — drift guards, fingerprint unification, modal metrics, LegalBench subset handling** — Owner `modal-specialist (opencode)` — DELIVERED (commits `81f57b42`, `f933ff02`, `0d425ad5`, `338aa529`, `5837953b`): LegalBench fix set from the lucius audit (job runner `expected` reads `answer` — the old field scored every legalbench prediction wrong; loud missing fixtures; task/suite selection with seeded sampling; job guard; honest md5 mock labelled `mock/mock-legalbench`; suite bridge with `fetch_full_cuad.py` guidance); `dataset_sha256` verified at worker start and at resume with drift refusal; `--force` re-lock archives the old generation (`write_lock` refused to overwrite — stale lock under new dataset bytes); lock records `revision_resolved` + `prompt_text_sha` and refuses prompt-text drift; fingerprint unification (one canonical `dataset_fingerprint` shared by eval + job records); modal records stamped `serving_kind=modal` (the dojo classifier has no modal category), ok-only latency, champion cost table (`Qwen/Qwen3-8B` costed None before), ok-only scoring with `error_count`; GT-shard-absent refusal (never unlabeled rows); `expected_stage`/`question`/`answer` carried through `normalize_rows`. **Verified:** sandbox suite 190 passed / 1 skipped (24 new tests across `test_live_or_loud`, `test_legalbench`, `test_cli_wiring`, `test_job_remote`, `test_job_preflight`, `test_job_metrics`). Issue [#52](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/52) closed 2026-09-10.
- **DMR-051** (done 2026-09-10) — **Serving & container parity — llm-mailroom Modal app knobs, compose/producer env, models.yaml corrections** — Owner `modal-specialist (opencode)` — DELIVERED (commit `9bba7872`): llm-mailroom `deploy/modal_vllm.py` ports `MODAL_VLLM_TP_SIZE` (derived from the GPU `:N` suffix), scaledown/startup knobs, `download_model` pre-warm + slim image, and fixes the malformed guidance URL; producer compose forwards `VLLM_BASE_URL`/`VLLM_API_KEY` (a `DEFAULT_PROVIDER=vllm` producer used to resolve in-container localhost); sandbox compose gains `VLLM_TP_SIZE` substitution and jupyter `VLLM_API_KEY`; Dockerfile `[hf]` extra; MRV2 overstatement corrected (vllm-specialist verified the pinned models already run MRV2 in v0.28.0) + CHTC `-cu129` fallback note; `config/models.yaml` honest L4-bf16 KV-pool caps, `-FP8` checkpoint rows (verified live), A100 FP8-emulation caveat. **Verified:** both Modal apps import against `modal==1.5.5` with TP2 argv; `docker compose --profile vllm config` (81 lines) + jupyter config green; sandbox suite 190 passed / 1 skipped; llm-mailroom Modal capability 12 passed. Issue [#54](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/54) closed 2026-09-10.
- **DMR-052** (done 2026-09-10) — **Pipeline provider seam & prompt policy — vLLM guard, FREE_ONLY exemption, champion remap, Modal 503 backoff, prompt/corpus pins** — Owner `modal-specialist (opencode)` — DELIVERED (commit `1bf5bc7b`): `DEFAULT_PROVIDER=vllm` without `VLLM_BASE_URL` warns at resolution; the free-only guardrail exempts self-hosted providers (it bounds OpenRouter spend); taxonomy `vllm_model_map` remaps champion ids to served HF ids (a standalone vLLM run 404'd before); Modal 503s use a long bounded cold-start backoff (90s base / 240s cap); `_bound_prompt_versions` aligned to `contracts_specialist_v33`; `hf_corpus_loader.load_corpus` defaults to the pinned `FULL_CORPUS_REVISION`; sandbox prompt-registry falls back to code-default for a floating langfuse label without credentials (a pinned version still refuses); `run_id` rides into sandbox `run_pipeline` calls (catalog provenance); corpus-eda intent backfill resolves `VLLM_API_KEY`/`VLLM_BASE_URL` when `OPENROUTER_API_KEY` is absent. **Verified:** llm-mailroom 986 passed / 36 skipped; sandbox 190 passed / 1 skipped; corpus-eda exit 0. ⚠️ Two PRE-EXISTING llm-mailroom failures flagged (reproduce at the committed baseline, traced to the parallel agent's docclass mirror resync — `test_watcher_dedup` file-order + `VALID_CLASSES` len 6≠7): they belong to the DMR-016 resync surface, not this sweep. Issue [#55](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/55) closed 2026-09-10.

- **DMR-016** (done 2026-09-10) — **NEW judge/reviewer/arbiter/boss prompt keys + The-Mailroom vendored-mirror resync** — Owner `opencode (GLM-5.3-Flash)` — DELIVERED (commits `61751b30` + `77a7ea11`): NEW docclass role keys registered — `reviewer_docclass_v2` (from DMR-015's interrupted work) and the role half of the `mailroom_prompts` lineage (`reviewer/arbiter/judge/judge_classification/judge_correctness/boss_mailroom_prompts_v0`) — eval defaults unchanged; vendored `The-Mailroom/mailroom_ui/docclass_prompts.py` mirror resynced 32→74 keys byte-identical (order-preserving) from the source registry — it had gone stale at the KANBAN-090 era missing 42 keys (KANBAN-101/103, HUB-041 era, DMR-015 lineage); count pin + CHANGELOG updated. **Verified:** The-Mailroom suite 334 passed / 2 skipped. Residual (five-class + `unknown` grading language per plan §67 / extended-8-vs-five-class decision) spawned as **DMR-046** before close. Issue [#21](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/21) closed 2026-09-10.
- **DMR-015** (done 2026-09-10) — **Mailroom-named successor keys for the frozen pre-v8 insurance-subclass surfaces** — Owner `opencode (GLM-5.3-Flash)` — DELIVERED (commit `61751b30`, resync detail in `77a7ea11`): established the `mailroom_prompts` lineage (16 keys) teaching the v8 six-token insurance subclass set (carrier/pde/outpatient/inpatient/property/auto) aligned with the mailroom-corpus rename — sorter champion `sorter_mailroom_prompts_v0`, vision `sorter_mailroom_prompts_vision_v0` (via new `SORTER_DOCCLASS_VISION_PROMPT_V2` rule-40 LOB extension), pilot `sorter_mailroom_prompts_pilot_v0`, 7 `*_specialist_mailroom_prompts_v0`, and reviewer/arbiter/judge/judge_classification/judge_correctness/boss v0; new `_DOCCONTEXT_V2`/`_PILOT_CONTEXT_V1` context blocks + `_with_mailroom_prompts_context` derivation helper; reviewer gained `REVIEWER_DOCCLASS_PROMPT_V2`. Registries: PROMPT_VERSIONS 178, DOCCLASS_PROMPT_VERSIONS 74. Frozen constants and eval defaults untouched — same-surface A/Bs remain DMR-017. **Verified:** parity tests assert all 16 variants teach the 6-token set; llm-entity-extraction suite 757 passed / 28 skipped; `board_state.py check` green. Issue [#20](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/20) closed 2026-09-10. Note: code swept into the parallel agent's `61751b30` commit (content intact, message unrelated — HUB-024/027 precedent); no defect card per caller.
- **DMR-043** (done 2026-09-10) — **Sandbox docker-compose — jupyter build/volume resolution + vllm GPU reservation** — Owner `board-evidence-auditor (opencode)` — DELIVERED (commit `73aa807f`): jupyter `build.context` `.`→`..` and bind mount `.:/workspace`→`..:/workspace` (both were resolving to `deploy/`, breaking the build and shadowing `/workspace`); top-level `name: mailroom-sandbox`; vllm service gained the nvidia GPU device reservation (`deploy.resources.reservations.devices`, driver nvidia, capabilities gpu); jupyter bound to `127.0.0.1:8888` + `user: ${SANDBOX_UID:-1000}:${SANDBOX_GID:-1000}`; `.dockerignore`/Dockerfile comments fixed. **Verified:** `docker compose config` resolves for jupyter/vllm/ollama profiles (daemon-free); suite 151 passed / 1 skipped. Issue [#46](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/46) closed 2026-09-10.
- **DMR-045** (done 2026-09-10) — **Sandbox vLLM model catalog + `MODAL_VLLM_TP_SIZE` tensor-parallel knob** — Owner `board-evidence-auditor (opencode)` — DELIVERED (commit `73aa807f`): `config/models.yaml` gained the `modal_models:` deploy matrix (13 entries — Qwen3 8B/14B/30B-A3B/32B ±AWQ/FP8, DeepSeek-R1-Distill 7B/8B/14B, Llama-3.1-8B + 3.3-70B gated, Mistral-Small-3.1-24B, Phi-4-mini) with per-GPU rows (L4/A10G/A100-40/80/H100), quantization, `max_model_len`, `tp_size`, and notes (14B-trap, gated repos, 70B TP2 path). `deploy/modal_vllm.py` gained the `MODAL_VLLM_TP_SIZE` knob → `--tensor-parallel-size`, default derived from the `:N` GPU suffix (1 for single GPU), travelling through the deploy Secret; `deploy/README.md` documents the knob + model matrix. **Verified:** suite 151 passed / 1 skipped (7 new tests: TP default/derived/override/Secret, compose context/mount/GPU/port/user). Issue [#48](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/48) closed 2026-09-10.
- **DMR-041** (done 2026-09-10) — **Sandbox job CLI — wire whole-run eval task delegation** — Owner `board-evidence-auditor (opencode)` — DELIVERED (commit `1085c273`): `job/runner.py` gained `_run_whole_run()` dispatching `pipeline`/`extract`/`chained`/`local_vs_api`/`isolated` to the existing public eval runners with lock-derived kwargs; `run_job` branches to it before the per-item loop (previously raised `ValueError: task <X> is not a per-item task in v1` for every advertised whole-run task). Terminal checkpoint + event on completion; failures write a `failed` checkpoint with the delegated error. `docs/jobs.md` now lists the precise whole-run task set. **Verified:** suite 144 passed / 1 skipped; CLI end-to-end `sandbox run start` on a `pipeline` spec → `state=done`, `cursor=10/10`, exit 0. Issue [#44](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/44) closed 2026-09-10.
- **DMR-042** (done 2026-09-10) — **Sandbox corpus Hub loader — fix `load_hf` NameError + `effective_revision` AttributeError** — Owner `board-evidence-auditor (opencode)` — DELIVERED (commit `1085c273`): `corpus.py:228` `load_hf` → `load_hf_rows` (the HF path — the DEFAULT for run specs — was completely broken); `spec.effective_revision()` → `spec.revision or FAMILY_HF_REVISION` (DatasetSpec has no such method); new `_resolve_revision()` trusts full 40-hex shas without a network call and re-raises non-sha resolution failures with context instead of silently floating; `preflight.py:124` `spec.dataset.effective_revision()` → `spec.effective_revision()` (RunSpec method); `mailroom-corpus-eda/hf_interface.py` gained the missing `hf_hub_download` import (latent NameError in `download_file`). **Verified:** Hub-path tests added (merge+verify, tampered-sha raises, prepare roundtrip, bad-tag raises, preflight pins sha); suite 144 passed / 1 skipped; corpus-eda 69 passed / 7 skipped. Issue [#45](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/45) closed 2026-09-10.
- **DMR-011** (done 2026-09-10) — **Harden `sync_packages.py patch_push` — committed-blob extraction + race caveat retirement** — Owner `opencode` — DELIVERED (DMR-028): `git ls-tree -r -z HEAD` + `git cat-file blob` replaces `git ls-files` + `shutil.copy2`; HUB-044 race caveat retired from `AGENTS.md` and `docs/wiki/Sub-Package-Sync.md`. Issue #16 closed 2026-09-10.
- **DMR-039** (done 2026-09-10) — **Comprehensive offline sandbox documentation** — Owner `opencode` — DELIVERED: `docs/SANDBOX-GUIDE.md` (14-section complete reference: prerequisites, setup, configuration, provider profiles, full CLI reference, Docker/Jupyter, evaluations, job system, Modal deployment, SSH tunnels, tracing, datasets, metrics, troubleshooting), `docs/wiki/Sandbox-Jobs.md` (job runner deep-dive), `docs/wiki/Sandbox-Modal.md` (Modal deployment guide), updated `docs/wiki/Offline-Sandbox.md` (expanded with quick-start checklist, CLI reference, all features), `_Sidebar.md` nav updated with new pages.
- **DMR-040** (done 2026-09-10) — **Audit finding incorrect** — Owner `board-evidence-auditor` — Claimed `vllm-specialist.md:83-84` still references `--disable-log-requests` — **factually wrong**: lines 83-84 already have `--no-enable-log-requests` (correct flag). DMR-031 fix already covered this line. Card closed, no action needed.
- **DMR-023** (done 2026-09-10) — **Modal deploy test stub fix — llm-mailroom** — Owner `opencode` — FIXED: `packages/llm-mailroom/src/tests/test_vllm_modal_capability.py` line 30 `_Secret.from_local` → `_Secret.from_dict`. Deploy file already fixed (DMR-029); test mock now matches the live API.
- **DMR-024** (done 2026-09-10) — **Modal deploy test stub fix — llm-entity-extraction** — Owner `opencode` — FIXED: `packages/llm-entity-extraction/tests/test_kanban096_modal_vllm.py` line 44 `_Secret.from_local` → `_Secret.from_dict`. Deploy file already fixed (DMR-030); test mock now matches the live API.
- **DMR-037** (done 2026-09-10) — **Merged into DMR-018** — Owner `board-evidence-auditor` — INSURBIAS v9 evaluation duplicate. Audit finding confirmed the gap (no evaluation exists); work scope already covered by DMR-018.
- **DMR-036** (done 2026-09-10) — **Merged into DMR-017** — Owner `board-evidence-auditor` — GEPA mailroom_v docclass lineage duplicate. Audit finding confirmed the gap (only pilot variants exist); work scope already covered by DMR-017.
- **DMR-035** (done 2026-09-10) — **Merged into DMR-014** — Owner `board-evidence-auditor` — Specialist mutation operators duplicate. Audit note **corrected 2026-09-14 (mailroom-issues#50):** the original finding claimed "no code exists" but `scripts/prompt_engineer.py` (structured mutation operators) + `config/prompt_engineer/agents.yaml` (registry) already ship in llm-entity-extraction; the true remaining gap is the fitness/evolution pass. Work scope covered by DMR-014.
- **DMR-034** (done 2026-09-10) — **Merged into DMR-016** — Owner `board-evidence-auditor` — Five-class docclass prompt keys duplicate. Audit finding confirmed the gap (extended-8 grading persists); work scope already covered by DMR-016.
- **DMR-033** (done 2026-09-10) — **Merged into DMR-015** — Owner `board-evidence-auditor` — Insurance subclass successor keys duplicate. Audit finding confirmed the gap (4-token set persists); work scope already covered by DMR-015.
- **DMR-032** (done 2026-09-10) — **Audit finding — DMR-026 gap: align htcondor vLLM templates** — Owner `vllm-specialist (opencode)` — VERIFIED + FIXED: both `.sub` files now pin `v0.28.0` (lines 25/33); `serve_vllm.sh:16` uses `--no-enable-log-requests`. Commit `c69f1537`.
- **DMR-031** (done 2026-09-10) — **Audit finding — DMR-025 gap: refresh vllm-specialist.md version policy** — Owner `vllm-specialist (opencode)` — VERIFIED + FIXED: `.opencode/agents/vllm-specialist.md` line 37 states v0.29.0 current stable, v0.28.0 family pin; lines 41-44 document the `--enable-log-requests` rename and Model Runner V2 change.
- **DMR-030** (done 2026-09-10) — **Audit finding — DMR-024 gap: port Modal vLLM fixes to llm-entity-extraction** — Owner `modal-specialist (opencode)` — VERIFIED + FIXED: `Secret.from_dict` (line 90), image `v0.28.0` (line 66), `--no-enable-log-requests` (line 133), `entity-vllm-cache` Volume (line 113), `MODAL_VLLM_REVISION` (lines 70/89/155-157), `modal==1.5.5` pin.
- **DMR-029** (done 2026-09-10) — **Audit finding — DMR-023 gap: port Modal vLLM fixes to llm-mailroom** — Owner `modal-specialist (opencode)` — VERIFIED + FIXED: `Secret.from_dict` (line 96), image `v0.28.0` (line 69), `--no-enable-log-requests` (line 149), `mailroom-vllm-cache` Volume (line 101), `MODAL_VLLM_REVISION` (lines 58/77/143-145), `modal==1.5.5` pin.
- **DMR-028** (done 2026-09-10) — **Fix `sync_packages.py patch_push` race condition** — Owner `opencode` — DELIVERED: replaced `git ls-files` + `shutil.copy2` (copies from working tree, propagating uncommitted changes) with `git ls-tree -r -z HEAD` + `git cat-file blob` (extracts committed blobs only). Retired the HUB-044 race caveat from `AGENTS.md` and `docs/wiki/Sub-Package-Sync.md`. **Verified:** `board_state.py check` 0/0, `scripts/sync_packages.py` no longer touches the working tree during `patch_push`.
- **DMR-027** (done 2026-09-10) — **Sandbox job CLI (`sandbox run`) — vLLM + Modal + OTLP trace sink with locked preflight, resumable checkpoints, job polling, and local↔Modal↔API metrics** — Owner `modal-specialist (opencode)` — DELIVERED (commit `9a988f9`): a spec-driven, resumable eval runner in `packages/local-mailroom-sandbox`. **New code:** `src/mailroom_sandbox/job/` (`spec.py`, `checkpoint.py`, `preflight.py`, `runner.py`, `otel.py`, `metrics.py`, `remote.py`); `src/mailroom_sandbox/corpus.py`; `src/mailroom_sandbox/prompt_registry.py`; `deploy/modal_job.py` worker. **CLI:** `sandbox run preflight|start|status|resume|cancel|list`, `sandbox prompts list|show`, `sandbox metrics compare --runs/--log`. **Verified:** full suite `135 passed / 1 skipped`. Issue [#32](https://github.com/LLM-Mailroom-Services/Digital-Mailroom/issues/32) closed 2026-09-10.
- **DMR-026** (done 2026-09-10) — **Align htcondor vLLM templates** — Owner `vllm-specialist (opencode)` — VERIFIED + FIXED by DMR-032: both `.sub` files now pin `v0.28.0`, `serve_vllm.sh` uses `--no-enable-log-requests`.
- **DMR-025** (done 2026-09-10) — **Refresh vllm-specialist.md version policy** — Owner `vllm-specialist (opencode)` — VERIFIED + FIXED by DMR-031: v0.29.0 current stable, v0.28.0 family pin, `--enable-log-requests` rename documented.
- **DMR-022** (done 2026-09-09) — **vLLM v0.28.0 serving sweep — log-flag rename, memory posture, local↔Modal parity** — Owner `vllm-specialist (opencode)` — DELIVERED (commit `bf9430a`): the vLLM-specific half of the sandbox hardening, delegated to the vllm-specialist and verified by the caller. **Boot-breaking fix:** `--disable-log-requests` does not exist in vLLM v0.28.0 (per-request logging is now opt-in `--enable-log-requests`) — both the Modal argv and the compose command now use `--no-enable-log-requests`; the newly pinned image would otherwise have rejected its own argv at boot. **Memory posture:** `--gpu-memory-utilization 0.90` (below vLLM's 0.92 default) and `--max-num-seqs 256` (vLLM's L4/OpenAI-server default), env-overridable via `MODAL_VLLM_GPU_MEMORY_UTILIZATION`/`MODAL_VLLM_MAX_NUM_SEQS` (compose `VLLM_GPU_MEMORY_UTILIZATION`/`VLLM_MAX_NUM_SEQS`). **Secret passthrough fix:** `MODAL_VLLM_REVISION` + both new knobs joined `CONFIG_ENV_KEYS` — the container re-imports the module, so deploy-time env must travel via the Secret or argv silently falls back to defaults. **Compose parity:** same image tag and argv, `VLLM_MODEL`/`VLLM_MAX_MODEL_LEN` substitution, server-side bearer `VLLM_API_KEY` and `HF_TOKEN` env matching Modal. **Version decision:** keep v0.28.0 in both pins — v0.29.0 (2026-09-09) flips Model Runner V2 to the default for all models with zero soak time; bump both after a live parity run. **Deliberately not added:** prefix caching/chunked prefill (default-on in v0.28.0), async scheduling (experimental), `--enforce-eager`, `--served-model-name`, `--guided-decoding-backend` (replaced by `--structured-outputs-config`), `--swap-space` (removed), KV-cache fp8. **Tests:** `tests/test_modal_vllm.py` 18 → 25 (flag-rename regression, memory env overrides, Secret passthrough, `TestComposeParity`), package suite 83 passed / 1 skipped; `docker compose --profile vllm config` validates.
- **DMR-021** (done 2026-09-09) — **Offline sandbox Modal best-practice hardening — SDK 1.5.5 pin, `Secret.from_local` removal fix, vLLM v0.28.0 image, weight pre-warm, compile-cache Volume, cost guards, contract tests** — Owner `modal-specialist (opencode)` — DELIVERED (commit `0eea320`, pushed): the Modal deploy surface in `packages/local-mailroom-sandbox` is now SDK-1.5.5-valid and pinned. **Critical fix:** SDK 1.5.5 removed `Secret.from_local`, which `deploy/modal_vllm.py` used — the app would have failed at import/deploy; knobs now use `Secret.from_dict` (skips absent keys, preserving optional `HF_TOKEN`/`MODAL_VLLM_API_TOKEN`; `from_local_environ` raises on missing). **Pins:** `modal==1.5.5` in the `[deploy]` extra (was `>=0.73`) + `uv.lock` re-locked; image default `vllm/vllm-openai:v0.28.0` (was `latest`) matching the local compose pin (`v0.29.0` newest upstream stable, noted for a deliberate bump); optional `MODAL_VLLM_REVISION`. **Cold start:** `sandbox-vllm-cache` Volume at `/root/.cache/vllm` (JIT/CUDA graphs) + CPU-only `download_model` pre-warm (`modal run deploy/modal_vllm.py::download_model`, HF cache Volume + commit) + `--check` bearer-aware `/models` probe in the local entrypoint. **Cost discipline:** App cost-allocation tags; `max_containers=1` / `min_containers=0` defaults; `MODAL_VLLM_SCALEDOWN_SECONDS`/`MODAL_VLLM_MAX_CONTAINERS`/`MODAL_VLLM_MIN_CONTAINERS`/`MODAL_VLLM_STARTUP_TIMEOUT_SECONDS` knobs; verified pricing table (L4 ≈ $0.80/hr warm, scale-to-zero after 900 s) with deploy → verify → teardown → security → snapshots/decorator → troubleshooting in `deploy/README.md`. **Tests:** new `tests/test_modal_vllm.py` (18 network-free tests) pins argv, bearer env, secrets, volumes, cost guards, image pin, and regression-guards the removed API; package suite 76 passed / 1 skipped; app imports cleanly against real `modal==1.5.5` (scratch venv).
- **DMR-020** (done 2026-09-09) — **vLLM + Modal agents callable as primary agents and subagents** — Owner `opencode` — DELIVERED (commit `2708190`): `.opencode/agents/vllm-specialist.md` and `.opencode/agents/modal-specialist.md` now carry `mode: all` (were `mode: subagent`), so both are selectable as primary agents **and** callable through the Task tool as subagents; `opencode agent list` (fresh CLI process) reports `vllm-specialist (all)` + `modal-specialist (all)`. `AGENTS.md` roster intro + `docs/wiki/Subagents.md` document the primary+subagent availability. Issue #25 closed 2026-09-09.
- **DMR-019** (done 2026-09-09) — **Adopt the `unassigned` lane — unclaimed cards free to claim** — Owner `opencode` — DELIVERED (commit `6e8d02b`): the served board's five-lane flow is now the canonical board's too — `scripts/board_state.py` `LANES`/`LANE_LABELS` gain `unassigned`/`stage/unassigned` with lane-owner coherence rules; `.github/labels.json` (33-label manifest, synced live) + `hub_card.yml` add the lane; `governance/TASKS.md` documents five lanes. Issue #24 closed 2026-09-09.
- **DMR-010** (done 2026-09-09) — **HUB → DMR open-issue sync — 8 still-open HUB cards mirrored with synced issues + cross-links** — Owner `opencode` — DELIVERED (commit `570f2b9`): the 9 open `kanban` issues on the predecessor board were verified against this checkout; 8 carried open work and are mirrored as DMR-011..DMR-018 (#16–#23). Issue #15 closed 2026-09-09.
- **DMR-009** (done 2026-09-09) — **Repo hygiene + wiki publication — dead-branch prune, residual Speed Insights lockfile delta, subagents + board configs on the wiki** — Owner `opencode` — DELIVERED (commit `da0ea35`): all 8 dead remote branches deleted; wiki published for the first time (14 pages). Issue #13 closed 2026-09-09.
- **DMR-008** (done 2026-09-09) — **Specialist subagent roster in AGENTS.md + dedicated vLLM/Modal subagents** — Owner `opencode` — DELIVERED (commit `468f731`): `AGENTS.md` carries the specialist roster section; new project subagents `.opencode/agents/vllm-specialist.md` and `.opencode/agents/modal-specialist.md`. Issue #12 closed 2026-09-09.
- **DMR-007** (done 2026-09-09) — **Retire stray `board-site` Vercel project + served-board write-path proof** — Owner `human` — DELIVERED (card commit `f3a4042`): the duplicate project `board-site` was deleted via the Vercel API. Issue #11 closed 2026-09-09.
- **DMR-006** (done 2026-09-09) — **Org-migration reference audit + wiring sweep + CI gate** — Owner `opencode` — DELIVERED (commit `849663a`): `docs/reports/audits/org_migration_audit.md` + `.json`, new `scripts/audit_references.py`, wired into CI. Issue #10 closed 2026-09-09.
- **DMR-004** (done 2026-09-09) — **Push-triggered deploys for the served board — Vercel Git integration** — Owner `opencode` — DELIVERED (commit `4347055`): Vercel GitHub App installed, project `digital-mailroom` linked. Issue #8 closed 2026-09-09.
- **DMR-001** (done 2026-09-09) — **Standalone board bootstrap — fresh DMR TASKS.md + tooling re-point** — Owner `opencode` — DELIVERED (commit `be75d1e`): fresh DMR board + full tooling re-point to `LLM-Mailroom-Services/Digital-Mailroom`. Issue #5 closed 2026-09-09.
- **DMR-002** (done 2026-09-09) — **Served dispatch board for the standalone clone — new Vercel project** — Owner `opencode` — DELIVERED (commit `be75d1e`): new Vercel project `digital-mailroom`. Issue #6 closed 2026-09-09.
- **DMR-003** (done 2026-09-09) — **Original mailroom-dev board access — write path + deployment URL** — Owner `human` — DELIVERED (commit `be75d1e`): live write path verified. Issue #7 closed 2026-09-09.
- **DMR-005** (done 2026-09-09) — **Cross-board navigation tabs — DMR ↔ HUB served boards** — Owner `human` — DELIVERED (human directive 2026-09-09): board-switcher cross-links both served boards. Issue #9 closed 2026-09-09.
